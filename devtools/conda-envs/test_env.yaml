name: test

channels:
  - conda-forge

dependencies:
  # Base depends
  - python
  - pip

  # molsystem requires, and only available via Conda.
  - openbabel
  - py-cpuinfo

  # Testing
  - codecov
  - flake8
  - pytest
  - pytest-cov

    # Pip-only installs
  - pip:
    - seamm
    - seamm-widgets

      # Testing
    - yapf
