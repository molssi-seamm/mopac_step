name: test

channels:
  - default
  
dependencies:
    # Base depends
  - python
  - pip
  - configargparse
  - py-cpuinfo

    # Testing
  - coverage
  - flake8
  - pytest
  - pytest-cov
  - pytest-runner

    # Pip-only installs
  - pip:
    - seamm>=0.6
    - seamm-widgets>=0.6

      # Testing
    - codecov
    - yapf
